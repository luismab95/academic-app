import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  errorText: {
    color: 'red',
    fontSize: 11,
  },
  errorContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 10,
    marginLeft: 16,
  },
});
